[pi_nodes]
pi-01 ansible_host=172.30.80.22

# ansible_user, become, and become_method are set in ansible.cfg.
# Only add host-specific connection overrides here if a device differs from the defaults.
